<?xml version="1.0"?>
<configuration>
  <startup>
    <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.5"/>
  </startup>

  <system.net>
    <mailSettings>
      <!-- May I humbly suggest using Papercut for SMTP testing? http://papercut.codeplex.com -->
      <smtp deliveryMethod="SpecifiedPickupDirectory">
        <network host="localhost"/>
          <specifiedPickupDirectory pickupDirectoryLocation="c:\email"/>
      </smtp>
    </mailSettings>
  </system.net>

</configuration>
